// Lazy-loaded feature bundle for framer-motion.
// Kept in a separate module so `LazyMotion` can dynamic-import it
// off the initial critical path.
export { domAnimation as default } from 'framer-motion';
